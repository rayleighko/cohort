---
description: Day-end retrospective — capture learnings to memory and update tracker
allowed-tools: Read, Write, Edit, Bash
---

At Day end, capture learnings:

1. **What worked well today?** (1-3 bullets)
2. **What surprised us?** (1-3 bullets — vault drift, blocker, new pattern)
3. **What anti-pattern almost happened?** (0-2 bullets — caught vs. would-have)
4. **What memory file should be created or updated?**
   - If new locked decision → create new `~/Development/cohort/.claude/memory/<topic>.md` (project type)
   - If new collaboration rule → create new `<rule>.md` (feedback type)
   - If existing memory needs update → edit in place
   - Update `MEMORY.md` index for new files
5. **What vault drift to fix?** — note in END_OF_TURN Block D

Output: concise markdown summary with these 5 sections. Add to commit message of final push if applicable.
