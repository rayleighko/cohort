global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Uncomment when Next.js exposes /api/metrics (v2)
  # - job_name: cohort-web
  #   static_configs:
  #     - targets: ["host.docker.internal:3000"]
