/**
 * Encryption helpers.
 * V1 relies on Supabase at-rest encryption + RLS for stored data.
 * This module is reserved for any app-level field encryption added later.
 * TODO(W4-W5): implement if a field needs encryption beyond Supabase at-rest.
 */

export {};
