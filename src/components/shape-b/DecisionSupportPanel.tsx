/**
 * Shape B — 분할매수 Decision Support panel.
 * Displays Macro/Technical/Sentiment 3-score breakdown + 본인 plan reference.
 * Decision support only — never a buy/sell recommendation (Option B).
 * TODO(W3-W4): build per-ticker score display.
 */
export default function DecisionSupportPanel() {
  return null;
}
