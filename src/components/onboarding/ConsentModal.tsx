'use client';

/**
 * PIPA consent modal — onboarding consent flow.
 * Per 20-sim-real-verification §11.8. Scaffold Day 1; built Day 2.
 * Collects: consent_analytics / consent_interview / consent_kakao_notification.
 * 즉시 삭제 옵션 must remain accessible afterward (Settings > 데이터 관리).
 * TODO(Day 2): build consent checkboxes + Privacy/ToS links + user_profile write.
 */
export default function ConsentModal() {
  return null;
}
