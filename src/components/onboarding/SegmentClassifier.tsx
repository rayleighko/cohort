/**
 * Sub-cluster classifier — auto B.1.a / B.1.b / B.1.c assignment from survey answers.
 * TODO(W4): classification logic + user_profile.sub_cluster write.
 */
export default function SegmentClassifier() {
  return null;
}
