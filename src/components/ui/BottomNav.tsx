/**
 * Mobile bottom navigation — fixed, used inside the (dashboard) layout.
 * TODO(W2-W3): wire tabs (대시보드 / Shape A / Shape B / Shape C / 설정) + active state.
 */
export default function BottomNav() {
  return null;
}
