/**
 * Web Push opt-in prompt — shown at Tier 0/1 trial start.
 * TODO(W4): request notification permission, POST subscription to /api/push/subscribe.
 */
export default function PushPrompt() {
  return null;
}
