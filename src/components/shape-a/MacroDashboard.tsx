/**
 * Shape A — Macro Indicator Dashboard (한국 + 미국 매크로 통합 + composite score).
 * TODO(W2-W3): 15-20 indicators (Tier 0) → 50+ indicators + custom widgets (Tier 2).
 */
export default function MacroDashboard() {
  return null;
}
