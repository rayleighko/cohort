'use client';

/**
 * PWA install prompt (Add to Home Screen).
 * TODO(W5): capture `beforeinstallprompt`, show a Cohort-branded install CTA,
 * track acceptance in PostHog.
 */
export default function InstallPrompt() {
  return null;
}
