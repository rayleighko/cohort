/**
 * Shape C — 24-hour cooldown soft-pause modal (behavioral guard).
 * Vesper surfaces the trigger; the cooldown gives the user pause time.
 * TODO(W4-W5): build cooldown countdown + 본인 plan reference UX.
 */
export default function CooldownModal() {
  return null;
}
