/**
 * Supabase generated types — placeholder.
 * TODO(Day 2): regenerate via `pnpm supabase:types` after applying
 * supabase/migrations/0001_initial_schema.sql to the production project.
 */

export type Database = Record<string, never>;
