/**
 * Tier 0 Macro Dashboard (authenticated home).
 * TODO(W2): ECOS + FRED fetch + composite score + Aurora morning brief.
 */
export default function DashboardPage() {
  return (
    <main className="mx-auto max-w-md px-6 py-10">
      <h1 className="text-2xl font-bold text-cohort-charcoal">대시보드</h1>
      <p className="mt-2 text-sm text-cohort-charcoal/60">
        TODO(W2): Tier 0 매크로 대시보드.
      </p>
    </main>
  );
}
