/**
 * Settings — account + PIPA consent + 데이터 관리 (즉시 삭제 옵션 must live here).
 * TODO(Day 2 / W5): consent management + immediate data-delete option.
 */
export default function SettingsPage() {
  return (
    <main className="mx-auto max-w-md px-6 py-10">
      <h1 className="text-2xl font-bold text-cohort-charcoal">설정</h1>
      <p className="mt-2 text-sm text-cohort-charcoal/60">
        TODO: 계정 · PIPA 동의 · 데이터 관리(즉시 삭제).
      </p>
    </main>
  );
}
