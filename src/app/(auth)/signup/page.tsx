/**
 * Signup page. TODO(Day 2): Supabase Auth signup + user_profile upsert + ConsentModal.
 */
export default function SignupPage() {
  return (
    <main className="mx-auto max-w-md px-6 py-16">
      <h1 className="text-2xl font-bold text-cohort-charcoal">회원가입</h1>
      <p className="mt-2 text-sm text-cohort-charcoal/60">
        TODO(Day 2): Supabase Auth 가입 + PIPA 동의 플로우.
      </p>
    </main>
  );
}
