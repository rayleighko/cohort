/**
 * Macro domain facade — v2 incremental extract.
 * Source of truth remains `src/lib/macro/composite.ts` until full move.
 */
export {
  computeMacroComposite,
  type MacroComposite,
  type ComputeMacroCompositeInput,
} from '@/lib/macro/composite';
